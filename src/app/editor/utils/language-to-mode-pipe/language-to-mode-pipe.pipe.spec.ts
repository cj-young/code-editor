import { LanguageToModePipePipe } from './language-to-mode-pipe.pipe';

describe('LanguageToModePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageToModePipePipe();
    expect(pipe).toBeTruthy();
  });
});
